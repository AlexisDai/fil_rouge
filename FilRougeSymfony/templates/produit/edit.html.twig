{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block produit %}
    <h1>Edit Produit</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_produit_show', {'id': produit.rubrique.id}) }}">Retour</a>
         {# {'id': produit.rubrique.id} le chemin complet pour le retour #}

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
